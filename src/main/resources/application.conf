# Database configuration
db {
  dataSourceClassName = org.postgresql.ds.PGSimpleDataSource
  dataSource {
    user = "postgres"
    password = "postgres"
    databaseName = "nasb1995"
    portNumber = 5432
    serverName = "52.198.76.7"
  }
  # Connection pool settings
  connectionTimeout = 30000
  maximumPoolSize = 10
}

# HTTP server configuration
http {
  host = "localhost"
  port = 8277
}